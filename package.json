{
  "name": "code-snippet-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Code Snippet Manager - Full Stack Application",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

